[package]
name = "servicemanager-runner"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "NGSM service-host runner: SCM dispatcher entry point invoked as `ngsm.exe run-service <name>` that bridges SCM controls into the supervisor."
publish = false

[dependencies]
servicemanager-core = { workspace = true }
servicemanager-win32 = { workspace = true }
servicemanager-registry = { workspace = true }
servicemanager-supervisor = { workspace = true }
